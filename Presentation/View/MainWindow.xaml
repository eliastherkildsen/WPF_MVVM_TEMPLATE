<Window x:Class="WPF_MVVM_TEMPLATE.Presentation.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM_TEMPLATE"
        xmlns:viewModels="clr-namespace:WPF_MVVM_TEMPLATE.Presentation.ViewModel"
        xmlns:views="clr-namespace:WPF_MVVM_TEMPLATE.Presentation.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>

        <DataTemplate x:Name="blueViewTemplate" DataType="{x:Type viewModels:BlueViewModel}">
            <views:BlueView />
        </DataTemplate>
            
        <DataTemplate x:Name="redViewTemplate" DataType="{x:Type viewModels:RedViewModel}">
            <views:RedView/>
        </DataTemplate>
        
    </Window.Resources>
    
    <StackPanel>
        <Button Content="redView" Click="ButtonRedClick"></Button>
        <Button Content="blueView" Click="ButtonBlueClick"></Button>
        <ContentControl Content="{Binding CurrentViewModel}" />
    
    </StackPanel>
    
</Window>
